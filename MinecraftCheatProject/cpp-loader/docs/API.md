# C++ Loader API

## ConfigManager
- `load()` / `save()` for launcher settings.

## NetworkManager
- `getSync(url)` performs a blocking GET request.

## Notes
- TODO: Expand documentation with error model and threading constraints.
