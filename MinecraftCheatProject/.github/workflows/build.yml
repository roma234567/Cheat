name: Build

on:
  push:
    branches: [ "**" ]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate repository layout
        run: |
          test -f MinecraftCheatProject/cpp-loader/CMakeLists.txt
          test -f MinecraftCheatProject/kotlin-mod/build.gradle.kts
      - name: TODO placeholder
        run: echo "TODO: add Qt and Gradle build jobs"
